import styled from 'styled-components';

export const Button = styled.button`
    width: 25px;
    height: 30px;

    border: none;
    border-radius: 7px;
    outline: none;

    &:hover {
        cursor: pointer;
    }
`